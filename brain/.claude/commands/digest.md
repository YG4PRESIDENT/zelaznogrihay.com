---
description: Morning briefing — yesterday's ingests, 3 questions to think about, 1 synthesis suggestion.
allowed-tools: Read, Write, Edit, Bash(rg:*), Bash(grep:*), Bash(ls:*), Bash(date:*), Bash(find:*)
---

Run the Digest Workflow defined in CLAUDE.md section 11.

Cover the last 24 hours:
1. New raw items captured (count by source: apple-notes, gdocs, clips, voice, manual).
2. New wiki files created (list with titles).
3. Updated wiki files (list with titles).
4. 3 questions Leo should think about today, drawn from `## Open Questions` in index.md or unresolved threads in summaries.
5. 1 synthesis suggestion: 3+ summaries that share a theme. Name them and the theme.
6. Any urgent items from Memory.md (deadlines, open actions).

Format the digest as a single markdown document. Print to stdout. Also write it to `wiki/syntheses/digest-<YYYY-MM-DD>.md` and append a one-line summary to log.md.
